## 变量声明的基本格式

```java
// type identifier [ = value][, identifier [= value] ...] ;
// type 数据类型
// identifier 变量名

int a, b, c; // 声明三个变量
int d = 1, e = 2, f = 3; // 声明三个变量并赋值
int g = 1; // 声明单个变量并赋值
```
